'use client';

import { LogoutButton } from '@components/feature';

const AppSidebar = () => {
  return (
    <aside>
      <div>UserInfo</div>
      <LogoutButton />
    </aside>
  );
};

export default AppSidebar;
